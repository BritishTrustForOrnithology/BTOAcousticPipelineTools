# Run the following command to run the Tool app in a browser window

#path to where app is saved on your machine- update if necessary
path_to_app <- 'app.R'

#run the app
shiny::runApp(path_to_app, launch.browser = TRUE)
